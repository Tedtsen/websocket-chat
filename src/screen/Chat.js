import React from 'react';
import {View, Text} from 'react-native';

const Chat = ({navigation}) => {
  return (
    <View>
      <Text>This is the chat screen. Gotta use Gifted Chat later.</Text>
    </View>
  );
};

export default Chat;
